 实时探路 - 实现计划

 Context

 基于PRD文档和UI设计，实现一个基于位置的实时信息服务平台（移动端优先Web应用）。核心流程：发布者发布悬赏任务 → 附近用户接单 → 到现场拍照提交 →
 发布者确认 → 结算支付。

 技术栈

 - 前端：React + TypeScript + Vite + Ant Design Mobile
 - 地图：高德地图 JS API 2.0（Key已有）
 - 后端：Node.js + Express + TypeScript
 - 数据库：PostgreSQL + PostGIS（docker-compose）
 - 状态管理：Zustand
 - ORM：Knex.js
 - 实时通知：Socket.io
 - 文件上传：Multer（本地存储）
 - 支付：模拟钱包系统（预留真实支付接口）

 项目结构

 realtime-scout/
 ├── docker-compose.yml          # PostgreSQL + PostGIS
 ├── packages/shared/            # 共享类型和常量
 ├── apps/
 │   ├── server/                 # Express 后端
 │   │   ├── src/
 │   │   │   ├── routes/         # 路由
 │   │   │   ├── controllers/    # 控制器
 │   │   │   ├── services/       # 业务逻辑
 │   │   │   ├── middleware/     # auth, upload, validate
 │   │   │   ├── db/migrations/  # 数据库迁移
 │   │   │   └── socket/         # Socket.io
 │   │   └── uploads/            # 本地文件存储
 │   └── web/                    # React 前端
 │       └── src/
 │           ├── pages/          # 7个页面
 │           ├── components/     # 公共组件
 │           ├── hooks/          # 自定义hooks
 │           ├── services/       # API调用
 │           └── store/          # Zustand状态

 数据库设计

 - users - 用户表（balance, frozen_balance, rating）
 - tasks - 任务表（PostGIS location, status状态机, reward, deadline）
 - submissions - 提交表（照片+描述, GPS验证距离）
 - submission_photos - 提交照片表
 - transactions - 交易流水表（钱包账本）
 - notifications - 通知表

 任务状态机：pending → accepted → submitted → confirmed → completed，另有 expired 和 cancelled。

 API 端点

 - POST /auth/register|login - 注册登录
 - POST /tasks - 发布任务
 - GET /tasks/nearby?lng=&lat=&radius= - 附近任务（PostGIS）
 - POST /tasks/:id/accept - 接单
 - POST /tasks/:id/submit - 提交（multipart照片）
 - POST /tasks/:id/confirm|reject - 确认/拒绝
 - GET /tasks/my/published|accepted - 我的任务
 - POST /wallet/topup - 模拟充值
 - GET /wallet/transactions - 交易记录

 前端路由

 ┌──────────────────┬───────────────────────┬──────────────────┐
 │       路径       │         页面          │      对应UI      │
 ├──────────────────┼───────────────────────┼──────────────────┤
 │ /login           │ 登录                  │ -                │
 ├──────────────────┼───────────────────────┼──────────────────┤
 │ /                │ 首页（地图+任务列表） │ 首页.png         │
 ├──────────────────┼───────────────────────┼──────────────────┤
 │ /publish         │ 发布任务              │ 发布任务界面.png │
 ├──────────────────┼───────────────────────┼──────────────────┤
 │ /task/:id        │ 任务详情              │ 任务详情界面.png │
 ├──────────────────┼───────────────────────┼──────────────────┤
 │ /task/:id/submit │ 提交任务              │ 提交任务.png     │
 ├──────────────────┼───────────────────────┼──────────────────┤
 │ /browse          │ 接单页                │ 接单页.png       │
 ├──────────────────┼───────────────────────┼──────────────────┤
 │ /my-tasks        │ 我发布的              │ 我发布的.png     │
 ├──────────────────┼───────────────────────┼──────────────────┤
 │ /profile         │ 个人中心              │ 个人中心.png     │
 └──────────────────┴───────────────────────┴──────────────────┘

 底部TabBar：首页、接单、发布(+)、我的、个人

 关键业务逻辑

 1. 钱包系统：发布任务时冻结金额，确认后转给接单者，过期/取消则解冻退回
 2. GPS验证：提交时计算用户位置与任务位置的Haversine距离，>500m拒绝
 3. 任务过期：node-cron每分钟检查，过期任务自动关闭并退款
 4. 自动确认：提交后24小时未操作自动确认支付

 实现顺序

 1. 基础搭建 - monorepo、docker-compose、数据库迁移、Express/Vite项目初始化
 2. 认证模块 - 注册/登录API + 前端页面 + JWT + 路由守卫
 3. 任务发布 - 发布API + 钱包冻结 + 前端表单 + 高德地图选点
 4. 地图首页 - PostGIS附近查询 + 地图标记 + 任务列表
 5. 接单提交 - 接单API + 文件上传 + GPS验证 + 提交页面
 6. 确认结算 - 确认/拒绝 + 支付结算 + 交易记录
 7. 实时通知 - Socket.io + 通知推送 + 任务过期定时器

     6. 确认结算 - 确认/拒绝 + 支付结算 + 交易记录
     7. 实时通知 - Socket.io + 通知推送 + 任务过期定时器

     验证方式

     1. docker-compose up 启动数据库
     2. 后端 npm run dev 启动在 :3001
     3. 前端 npm run dev 启动在 :5173
     4. 浏览器打开前端，完整走通：注册 → 充值 → 发布任务 → 另一账号接单 → 提交照片 → 确认 → 查看余额变化

     前置要求

     用户需要安装：
     - Node.js 18+
     - Docker（用于PostgreSQL + PostGIS）
     - npm